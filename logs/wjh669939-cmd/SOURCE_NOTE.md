# SOURCE_NOTE

Guest VM Cursor Agent transcripts for **王筠昊** (`wjh669939-cmd`).

- Official `contest-snapshot --backfill --source cursor` imported **0** (Agent mode, no composer workspace mapping).
- Archived from `~/.cursor/projects/*/agent-transcripts/` (schema 1.0 + `raw/`).
- Skipped: 1-line empty session, gnome-rdp TLS chat, empty-window duplicate of `01d2fca0`.
- Historical `ghp_*` tokens in one old transcript were redacted before commit.
